version: "3.9"

services:
  self-intro:
    build: .
    container_name: self-intro
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped

